version: '3.4'

services:    
  takttusur.redis:
    image: redis:7.2.4-alpine
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - takttusur-redis:/data
        
volumes:
  takttusur-redis:
    driver: local
    

